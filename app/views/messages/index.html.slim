.row.messages
  .col-md-3
    - @messages.each do |message|
      -if message.sender_id == current_user.id
        .outing-messages
          p пользователь #{current_user.email} написал
          = message.body
      -else
        .incoming-messages
          p пользователь #{message.sender.email} написал
          = message.body

= render 'message'